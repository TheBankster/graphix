[settings]
tracing = true

[graphdb]
# host = "localhost"   # hostname of the GraphDB server (default: localhost)
# backend = "graphdb"  # storage backend: "graphdb" (default) or "rdflib" (local, no server required)
